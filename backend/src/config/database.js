const db = {
  host: "localhost",
  username: "root",
  password: "Flavio#0812",
  schema: "ppi",
  dialect: "mysql",
  database: "ppi",
  define: {
    timestamps: true,
    underscored: true
  }
}
export default db